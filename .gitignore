# Scan outputs (written next to the scripts by default) — never commit these:
# they can contain secrets and live payloads.
miasma-shaihulud-audit-*/
miasma-persistence-*/
miasma-npm-supplychain-*/
miasma-run-all-*.log

# Pasted triage / console captures — may contain secrets pulled from configs.
audit-deep.txt
*-triage.txt
